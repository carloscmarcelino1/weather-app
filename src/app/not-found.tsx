import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Página não encontrada',
  description: 'A página que você está buscando não foi encontrada.',
};

const NotFound = () => <></>;

export default NotFound;
